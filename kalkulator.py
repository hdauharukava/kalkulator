def integerDivision(a, b):
    if b != 0:
        return a // b
    else:
        return f"You can't divide by zero!"

def division(a, b):
    if b != 0:
        return round(a / b, 3)
    else:
        return f"You can't divide by zero!"


def multiplication(a, b):
    return a * b
